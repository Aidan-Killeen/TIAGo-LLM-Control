# TIAGo-LLM-Control
This code lets the user control a THIAGo Robot by using a LLM to interface with ROS

To run, first catkin_make, then run "rosrun controls node"
Ensure roscore has been opened in a different terminal first
